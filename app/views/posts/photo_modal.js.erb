// fill in the photo modal with this posts image


$("#post-<%= @post.id %>").modal("show");
$("#post-<%= @post.id %> .modal-body").html("<%=j render partial: 'modalbody'%>");


// image_tag @post.photo.url, class: "img-responsive"
